name = "fortml"
version = "0.1.0"
description = "Differentiable machine learning models for modern Fortran"
author = "lazy-fortran"
maintainer = "lazy-fortran"
license = "MIT"
copyright = "Copyright 2026 lazy-fortran"

[dependencies]
fortnum = { path = "../fortnum" }
fortopt = { path = "../fortopt" }

[dev-dependencies]
test-drive = { git = "https://github.com/fortran-lang/test-drive.git" }

[build]
auto-tests = true

[fortran]
implicit-typing = false
source-form = "free"
